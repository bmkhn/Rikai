{
  "name": "rikai",
  "version": "0.2.0",
  "private": true,
  "description": "Chrome extension to translate raw manga directly on the page",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.6"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.7",
    "canvas": "^3.2.3",
    "esbuild": "^0.25.10",
    "onnxruntime-node": "^1.29.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
